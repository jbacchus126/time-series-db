ROOT
  PIPELINE
    FUNCTION(mockFetchLine)
      VALUE(100)
      TAG_KEY(name)
        TAG_VALUE(baseline)
      TAG_KEY(env)
        TAG_VALUE(prod)
    FUNCTION(mockFetchLine)
      VALUE(50)
      TAG_KEY(name)
        TAG_VALUE(threshold)
      TAG_KEY(env)
        TAG_VALUE(dev)
    FUNCTION(scale)
      VALUE(0.5)
    FUNCTION(sum)
    FUNCTION(alias)
      VALUE(TotalScaled)
